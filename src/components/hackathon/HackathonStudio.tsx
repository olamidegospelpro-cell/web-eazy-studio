import { useMemo, useRef, useState, type CSSProperties, type PointerEvent } from "react";
import { ArrowLeft, Bot, Code2, Eye, GripVertical, Lock, LockOpen, Maximize2, Minimize2, Monitor, MousePointer2, PanelLeftClose, PanelLeftOpen, PanelRightClose, PanelRightOpen, Play, RefreshCw, Send, Smartphone, Sparkles, Tablet, WandSparkles } from "lucide-react";
import { Button } from "@/components/ui/button";
import { Textarea } from "@/components/ui/textarea";
import { generateWithAiPrototype, refineBlock, type DemoBlock } from "@/lib/hackathon/ai-prototype";

type Viewport = "desktop" | "tablet" | "mobile";
const widths: Record<Viewport, number> = { desktop: 1120, tablet: 760, mobile: 390 };
const samplePrompt = "Create a modern website for a Nigerian solar and electrical company called Vicky Tech Innovations. Use green and charcoal. Include a strong hero, services, trust-building copy and a clear contact CTA.";

export function HackathonStudio({ onClose }: { onClose: () => void }) {
  const [prompt, setPrompt] = useState(samplePrompt), [blocks, setBlocks] = useState<DemoBlock[]>([]), [selectedId, setSelectedId] = useState<string | null>(null);
  const [viewport, setViewport] = useState<Viewport>("desktop"), [preview, setPreview] = useState(false), [generating, setGenerating] = useState(false), [refining, setRefining] = useState(false);
  const [refine, setRefine] = useState("Make this more premium and modern"), [primary, setPrimary] = useState("#22c55e"), [businessName, setBusinessName] = useState("WebEazy Demo");
  const [engineMode, setEngineMode] = useState<"remote" | "local-prototype" | null>(null), [leftOpen, setLeftOpen] = useState(true), [rightOpen, setRightOpen] = useState(true), [immersive, setImmersive] = useState(false);
  const selected = useMemo(() => blocks.find((b) => b.id === selectedId) ?? null, [blocks, selectedId]);
  const updateBlock = (id: string, patch: Partial<DemoBlock>) => setBlocks((current) => current.map((b) => b.id === id ? { ...b, ...patch } : b));

  const generate = async () => { if (!prompt.trim()) return; setGenerating(true); try { const r = await generateWithAiPrototype(prompt.trim()); setBlocks(r.blocks); setBusinessName(r.businessName); setPrimary(r.primary); setEngineMode(r.mode); setSelectedId(r.blocks.find((b) => b.type === "heading")?.id ?? r.blocks[0]?.id ?? null); } finally { setGenerating(false); } };
  const refineSelected = async () => { if (!selected || !refine.trim()) return; setRefining(true); try { updateBlock(selected.id, await refineBlock(selected, refine, primary)); } finally { setRefining(false); } };
  const exportHtml = () => { const html = buildExportHtml(businessName, blocks); const url = URL.createObjectURL(new Blob([html], { type: "text/html;charset=utf-8" })); const a = document.createElement("a"); a.href = url; a.download = `${businessName.toLowerCase().replace(/[^a-z0-9]+/g, "-").replace(/^-|-$/g, "") || "webeazy-site"}.html`; a.click(); URL.revokeObjectURL(url); };

  return <div className="fixed inset-0 z-[100] flex flex-col bg-[#090b10] text-white">
    {!immersive && <header className="flex h-14 shrink-0 items-center justify-between border-b border-white/10 bg-[#10131a] px-2 sm:px-3">
      <div className="flex min-w-0 items-center gap-1.5"><Button variant="ghost" size="icon" onClick={onClose} className="text-white hover:bg-white/10 hover:text-white"><ArrowLeft className="size-4" /></Button><div className="flex size-8 items-center justify-center rounded-lg bg-violet-500 font-bold">W.</div><div className="min-w-0"><div className="flex items-center gap-2"><span className="truncate text-sm font-semibold">WebEazy AI Studio</span><span className="rounded-full border border-emerald-400/30 bg-emerald-400/10 px-2 py-0.5 text-[10px] font-semibold uppercase tracking-wide text-emerald-300">Hackathon MVP</span></div><p className="truncate text-[10px] text-white/45">AI handles the complexity. You keep the creativity.</p></div></div>
      <div className="flex items-center gap-1.5"><div className="hidden items-center gap-1 rounded-lg border border-white/10 bg-black/20 p-1 md:flex">{(["desktop","tablet","mobile"] as Viewport[]).map((v) => <button key={v} title={v} onClick={() => setViewport(v)} className={`rounded-md p-1.5 ${viewport === v ? "bg-white/10 text-white" : "text-white/35 hover:text-white"}`}>{v === "desktop" ? <Monitor className="size-3.5" /> : v === "tablet" ? <Tablet className="size-3.5" /> : <Smartphone className="size-3.5" />}</button>)}</div><Button variant="outline" size="sm" onClick={() => setPreview((v) => !v)} className="border-white/15 bg-transparent text-white hover:bg-white/10 hover:text-white"><Eye className="mr-1.5 size-3.5" />{preview ? "Design" : "Preview"}</Button><Button variant="outline" size="sm" onClick={() => setImmersive((v) => !v)} className="hidden border-white/15 bg-transparent text-white hover:bg-white/10 hover:text-white lg:flex">{immersive ? <Minimize2 className="mr-1.5 size-3.5" /> : <Maximize2 className="mr-1.5 size-3.5" />}{immersive ? "Exit focus" : "Focus"}</Button><Button size="sm" onClick={exportHtml} disabled={!blocks.length} className="bg-white text-black hover:bg-white/90"><Code2 className="mr-1.5 size-3.5" />Export HTML</Button></div>
    </header>}

    <div className="relative flex min-h-0 flex-1">
      {!preview && leftOpen && <aside className="hidden w-[300px] shrink-0 flex-col border-r border-white/10 bg-[#10131a] lg:flex"><div className="border-b border-white/10 p-3"><div className="mb-2 flex items-center justify-between"><div className="flex items-center gap-2 text-xs font-semibold uppercase tracking-[0.16em] text-white/55"><Bot className="size-4 text-violet-300" />Ask WebEazy</div><button title="Collapse AI panel" onClick={() => setLeftOpen(false)} className="rounded-md p-1 text-white/35 hover:bg-white/5 hover:text-white"><PanelLeftClose className="size-4" /></button></div><Textarea value={prompt} onChange={(e) => setPrompt(e.target.value)} className="min-h-32 resize-none border-white/10 bg-black/20 text-xs text-white placeholder:text-white/25" placeholder="Describe the business, style, sections and goals..." /><Button className="mt-3 w-full bg-violet-500 text-white hover:bg-violet-400" onClick={() => void generate()} disabled={generating || !prompt.trim()}>{generating ? <RefreshCw className="mr-2 size-4 animate-spin" /> : <WandSparkles className="mr-2 size-4" />}{generating ? "Generating..." : blocks.length ? "Regenerate website" : "Generate website"}</Button><div className="mt-2 flex items-center justify-between text-[10px] text-white/35"><span>{engineMode === "remote" ? "● Live AI connected" : engineMode === "local-prototype" ? "○ Prototype fallback" : "○ AI ready"}</span><span>Human editable</span></div></div><div className="min-h-0 flex-1 overflow-auto p-3"><div className="mb-2 flex items-center gap-2 px-1 text-[10px] font-semibold uppercase tracking-[0.16em] text-white/35"><GripVertical className="size-3.5" />Layers</div>{blocks.length ? <div className="space-y-1">{blocks.map((b) => <button key={b.id} onClick={() => setSelectedId(b.id)} className={`flex w-full items-center gap-2 rounded-lg px-2.5 py-2 text-left text-xs transition ${selectedId === b.id ? "bg-violet-500/20 text-violet-100" : "text-white/55 hover:bg-white/5 hover:text-white"}`}><MousePointer2 className="size-3.5 shrink-0" /><span className="min-w-0 flex-1 truncate">{b.name}</span>{b.locked && <Lock className="size-3 text-white/35" />}</button>)}</div> : <div className="rounded-xl border border-dashed border-white/10 p-4 text-center text-xs text-white/35">Generate a website to create editable layers.</div>}</div></aside>}
      {!preview && !leftOpen && <button title="Open AI and layers" onClick={() => setLeftOpen(true)} className="absolute left-2 top-2 z-50 rounded-lg border border-white/10 bg-[#10131a]/95 p-2 text-white/60 shadow-lg backdrop-blur hover:text-white"><PanelLeftOpen className="size-4" /></button>}

      <main className="relative min-w-0 flex-1 overflow-auto bg-[#080a0f]">{preview ? <PreviewWebsite blocks={blocks} businessName={businessName} /> : <div className="min-h-full w-full p-2 sm:p-3 lg:p-4"><div className="relative mx-auto h-[calc(100vh-78px)] min-h-[700px] overflow-auto bg-white shadow-[0_20px_80px_rgba(0,0,0,.45)]" style={{ width: "100%", maxWidth: widths[viewport] }}><DesignCanvas blocks={blocks} selectedId={selectedId} onSelect={setSelectedId} onUpdate={updateBlock} onGenerate={() => void generate()} generating={generating} /></div></div>}</main>

      {!preview && rightOpen && <aside className="hidden w-[280px] shrink-0 flex-col border-l border-white/10 bg-[#10131a] xl:flex"><div className="flex items-center justify-between border-b border-white/10 px-4 py-3"><div className="text-[10px] font-semibold uppercase tracking-[0.16em] text-white/40">Properties</div><button title="Collapse properties" onClick={() => setRightOpen(false)} className="rounded-md p-1 text-white/35 hover:bg-white/5 hover:text-white"><PanelRightClose className="size-4" /></button></div>{selected ? <div className="min-h-0 flex-1 overflow-auto p-4"><div className="mb-4 rounded-xl border border-white/10 bg-white/[0.03] p-3"><div className="text-xs font-semibold">{selected.name}</div><div className="mt-1 text-[10px] uppercase tracking-wide text-white/35">{selected.type} · directly editable</div></div>{selected.type !== "shape" && <Field label="Text" value={selected.text} onChange={(text) => updateBlock(selected.id, { text })} multiline />}<div className="grid grid-cols-2 gap-2"><Field label="X" value={String(selected.x)} onChange={(v) => updateBlock(selected.id, { x: num(v, selected.x) })} /><Field label="Y" value={String(selected.y)} onChange={(v) => updateBlock(selected.id, { y: num(v, selected.y) })} /><Field label="Width" value={String(selected.width)} onChange={(v) => updateBlock(selected.id, { width: Math.max(50, num(v, selected.width)) })} /><Field label="Height" value={String(selected.height)} onChange={(v) => updateBlock(selected.id, { height: Math.max(30, num(v, selected.height)) })} /></div><Field label="Background" value={selected.background} onChange={(v) => updateBlock(selected.id, { background: v })} /><Field label="Text colour" value={selected.color} onChange={(v) => updateBlock(selected.id, { color: v })} /><Field label="Radius" value={String(selected.radius)} onChange={(v) => updateBlock(selected.id, { radius: Math.max(0, num(v, selected.radius)) })} /><Button variant="outline" className="mb-5 w-full border-white/10 bg-transparent text-white hover:bg-white/5 hover:text-white" onClick={() => updateBlock(selected.id, { locked: !selected.locked })}>{selected.locked ? <LockOpen className="mr-2 size-4" /> : <Lock className="mr-2 size-4" />}{selected.locked ? "Unlock element" : "Lock element"}</Button><div className="rounded-xl border border-violet-400/20 bg-violet-500/5 p-3"><div className="mb-2 flex items-center gap-2 text-xs font-semibold text-violet-100"><Sparkles className="size-4" />AI refine selection</div><Textarea value={refine} onChange={(e) => setRefine(e.target.value)} className="min-h-20 resize-none border-white/10 bg-black/20 text-xs text-white" /><Button size="sm" className="mt-2 w-full bg-violet-500 text-white hover:bg-violet-400" onClick={() => void refineSelected()} disabled={refining}>{refining ? <RefreshCw className="mr-2 size-3.5 animate-spin" /> : <Send className="mr-2 size-3.5" />}{refining ? "Applying..." : "Apply AI instruction"}</Button></div></div> : <div className="p-5 text-xs leading-relaxed text-white/35">Select an element on the canvas to edit its position, size, content and appearance.</div>}</aside>}
      {!preview && !rightOpen && <button title="Open properties" onClick={() => setRightOpen(true)} className="absolute right-2 top-2 z-50 rounded-lg border border-white/10 bg-[#10131a]/95 p-2 text-white/60 shadow-lg backdrop-blur hover:text-white"><PanelRightOpen className="size-4" /></button>}
    </div>
    {!immersive && <footer className="flex h-7 shrink-0 items-center justify-between border-t border-white/10 bg-[#10131a] px-3 text-[10px] text-white/35"><span>{blocks.length ? `${blocks.length} editable elements · ${businessName}` : "Ready for your prompt"}</span><span className="hidden sm:inline">Freeform canvas · move · resize · edit · lock · preview · export</span></footer>}
  </div>;
}

function DesignCanvas({ blocks, selectedId, onSelect, onUpdate, onGenerate, generating }: { blocks: DemoBlock[]; selectedId: string | null; onSelect: (id: string) => void; onUpdate: (id: string, patch: Partial<DemoBlock>) => void; onGenerate: () => void; generating: boolean }) {
  return <div className="relative min-h-full w-full bg-white">{blocks.length === 0 ? <div className="flex min-h-[700px] items-center justify-center p-8 text-center text-slate-900"><div className="max-w-md"><div className="mx-auto mb-5 flex size-14 items-center justify-center rounded-2xl bg-violet-100 text-violet-600"><Sparkles className="size-7" /></div><h2 className="text-2xl font-bold">Turn a business idea into an editable website.</h2><p className="mt-2 text-sm leading-relaxed text-slate-500">Generate the first draft, then drag, resize, rewrite, recolour and lock elements like a design tool.</p><Button className="mt-5 bg-violet-600 text-white hover:bg-violet-500" onClick={onGenerate} disabled={generating}>{generating ? <RefreshCw className="mr-2 size-4 animate-spin" /> : <Play className="mr-2 size-4" />}Generate demo website</Button></div></div> : blocks.map((b) => <CanvasBlock key={b.id} block={b} selected={selectedId === b.id} onSelect={() => onSelect(b.id)} onUpdate={(p) => onUpdate(b.id, p)} />)}</div>;
}

function CanvasBlock({ block, selected, onSelect, onUpdate }: { block: DemoBlock; selected: boolean; onSelect: () => void; onUpdate: (patch: Partial<DemoBlock>) => void }) {
  const drag = useRef<{ cx: number; cy: number; x: number; y: number } | null>(null), resize = useRef<{ cx: number; cy: number; w: number; h: number } | null>(null);
  const style: CSSProperties = { position: "absolute", left: block.x, top: block.y, width: block.width, height: block.height, background: block.background, color: block.color, borderRadius: block.radius };
  const down = (e: PointerEvent<HTMLDivElement>) => { e.stopPropagation(); onSelect(); if (block.locked) return; drag.current = { cx: e.clientX, cy: e.clientY, x: block.x, y: block.y }; e.currentTarget.setPointerCapture(e.pointerId); };
  const move = (e: PointerEvent<HTMLDivElement>) => { if (!drag.current || block.locked) return; onUpdate({ x: Math.max(0, Math.round(drag.current.x + e.clientX - drag.current.cx)), y: Math.max(0, Math.round(drag.current.y + e.clientY - drag.current.cy)) }); };
  const resizeDown = (e: PointerEvent<HTMLDivElement>) => { if (block.locked) return; e.stopPropagation(); resize.current = { cx: e.clientX, cy: e.clientY, w: block.width, h: block.height }; e.currentTarget.setPointerCapture(e.pointerId); };
  const resizeMove = (e: PointerEvent<HTMLDivElement>) => { if (!resize.current || block.locked) return; onUpdate({ width: Math.max(60, Math.round(resize.current.w + e.clientX - resize.current.cx)), height: Math.max(32, Math.round(resize.current.h + e.clientY - resize.current.cy)) }); };
  const cls = block.type === "heading" ? "px-2 text-5xl font-black leading-none tracking-tight" : block.type === "text" ? "px-2 text-lg leading-relaxed" : block.type === "button" ? "justify-center px-5 text-sm font-semibold shadow-lg" : block.type === "card" ? "border border-slate-200 px-6 text-xl font-bold shadow-sm" : "";
  return <div style={style} onPointerDown={down} onPointerMove={move} onPointerUp={() => { drag.current = null; }} className={`group flex touch-none select-none items-center overflow-hidden ${cls} ${selected ? "z-30 outline outline-2 outline-violet-500 outline-offset-2" : "z-10"} ${block.locked ? "cursor-not-allowed" : "cursor-move"}`}>{block.type !== "shape" && <span contentEditable={!block.locked} suppressContentEditableWarning onPointerDown={(e) => e.stopPropagation()} onBlur={(e) => onUpdate({ text: e.currentTarget.textContent || "" })} className="w-full outline-none">{block.text}</span>}{block.locked && <Lock className="pointer-events-none absolute right-2 top-2 size-3 opacity-50" />}{selected && !block.locked && <div onPointerDown={resizeDown} onPointerMove={resizeMove} onPointerUp={() => { resize.current = null; }} className="absolute bottom-0 right-0 size-4 cursor-se-resize rounded-tl-md border-l border-t border-violet-500 bg-white" />}</div>;
}

function PreviewWebsite({ blocks, businessName }: { blocks: DemoBlock[]; businessName: string }) {
  if (!blocks.length) return <div className="flex min-h-full items-center justify-center bg-white text-sm text-slate-400">Generate a website before previewing.</div>;
  const heading = blocks.find((b) => b.type === "heading")?.text ?? businessName, heroCopy = blocks.find((b) => b.name === "Hero copy")?.text ?? "A better way to move your business forward.", heroButton = blocks.find((b) => b.name === "Primary CTA"), about = blocks.find((b) => b.name === "About copy")?.text, cards = blocks.filter((b) => b.type === "card"), dark = blocks.find((b) => b.name === "Hero background")?.background ?? "#111827", primary = heroButton?.background ?? "#6d5dfc";
  return <div className="min-h-full w-full bg-white text-slate-900"><section className="min-h-[58vh] px-[7vw] py-[14vh]" style={{ background: dark, color: "white" }}><div className="mx-auto max-w-6xl"><div className="mb-5 inline-flex rounded-full border border-white/15 px-3 py-1 text-[10px] font-semibold uppercase tracking-[0.2em] text-white/60">Designed in WebEazy</div><h1 className="max-w-6xl text-5xl font-black tracking-tight md:text-8xl">{heading}</h1><p className="mt-6 max-w-3xl text-lg leading-relaxed text-white/65 md:text-2xl">{heroCopy}</p>{heroButton && <button className="mt-9 rounded-xl px-7 py-3.5 text-sm font-semibold text-white" style={{ background: primary }}>{heroButton.text}</button>}</div></section><section className="px-[7vw] py-[10vh]"><div className="mx-auto max-w-6xl">{about && <p className="max-w-5xl text-2xl font-medium leading-relaxed text-slate-700 md:text-4xl">{about}</p>}<div className="mt-12 grid gap-5 md:grid-cols-3">{cards.map((c, i) => <div key={c.id} className="rounded-2xl border border-slate-200 p-7"><div className="mb-10 text-xs font-semibold" style={{ color: primary }}>0{i + 1}</div><h3 className="text-xl font-bold">{c.text}</h3><p className="mt-2 text-sm leading-relaxed text-slate-500">Flexible, customer-focused service designed around real business needs.</p></div>)}</div></div></section></div>;
}

function Field({ label, value, onChange, multiline }: { label: string; value: string; onChange: (value: string) => void; multiline?: boolean }) { return <label className="mb-3 block"><span className="mb-1 block text-[10px] font-semibold uppercase tracking-wide text-white/35">{label}</span>{multiline ? <Textarea value={value} onChange={(e) => onChange(e.target.value)} className="min-h-20 resize-none border-white/10 bg-black/20 text-xs text-white" /> : <input value={value} onChange={(e) => onChange(e.target.value)} className="h-9 w-full rounded-md border border-white/10 bg-black/20 px-2.5 text-xs text-white outline-none focus:border-violet-400/50" />}</label>; }
function num(v: string, fallback: number) { const n = Number(v); return Number.isFinite(n) ? n : fallback; }
function buildExportHtml(name: string, blocks: DemoBlock[]) { const heading = blocks.find((b) => b.type === "heading")?.text ?? name, hero = blocks.find((b) => b.name === "Hero copy")?.text ?? "Built for your business.", about = blocks.find((b) => b.name === "About copy")?.text ?? "", button = blocks.find((b) => b.name === "Primary CTA"), cards = blocks.filter((b) => b.type === "card"), dark = blocks.find((b) => b.name === "Hero background")?.background ?? "#111827", primary = button?.background ?? "#6d5dfc"; const esc = (v: string) => v.replace(/[&<>\"]/g, (c) => ({ "&": "&amp;", "<": "&lt;", ">": "&gt;", '"': "&quot;" }[c] ?? c)); return `<!doctype html><html><head><meta charset="utf-8"><meta name="viewport" content="width=device-width,initial-scale=1"><title>${esc(name)}</title><style>body{margin:0;font-family:Inter,Arial,sans-serif;color:#0f172a}*{box-sizing:border-box}.hero{padding:110px 8%;background:${dark};color:#fff}.hero h1{font-size:clamp(48px,8vw,96px);line-height:.95;margin:0}.hero p{font-size:20px;line-height:1.6;max-width:700px;color:#cbd5e1}.btn{display:inline-block;margin-top:24px;background:${primary};color:#fff;padding:14px 20px;border-radius:12px;text-decoration:none;font-weight:700}.content{padding:80px 8%}.intro{font-size:clamp(24px,4vw,42px);line-height:1.35;max-width:1000px}.cards{display:grid;grid-template-columns:repeat(auto-fit,minmax(220px,1fr));gap:16px;margin-top:48px}.card{border:1px solid #e2e8f0;border-radius:18px;padding:28px}.card h3{margin:0;font-size:22px}</style></head><body><section class="hero"><h1>${esc(heading)}</h1><p>${esc(hero)}</p>${button ? `<a class="btn" href="#contact">${esc(button.text)}</a>` : ""}</section><section class="content"><div class="intro">${esc(about)}</div><div class="cards">${cards.map((c) => `<div class="card"><h3>${esc(c.text)}</h3></div>`).join("")}</div></section></body></html>`; }
